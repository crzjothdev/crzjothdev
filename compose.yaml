services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: crzjothdev/crzjothdev
    container_name: alphawolf
    ports:
      - 3000:3000
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development